# CLI

`lanctools` provides a command-line interface with two main commands:

- [`lanctools merge`](./merge.md) can combine multiple .lanc files into a single one
- [`lanctools convert`](./convert.md) can convert from external formats into .lanc

To get help, use the `--help` flag:

```bash
lanctools --help
lanctools merge --help
```
