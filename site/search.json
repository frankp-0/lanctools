{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"lanctools","text":"<p>Tools for working with phased local ancestry data stored in the <code>.lanc</code> file format, as defined by Admix-kit [Hou et al., 2024].</p> <p><code>lanctools</code> is designed to provide fast local ancestry queries and convenient conversion from external formats (e.g., FLARE [Browning et al., 2023] and RFMix [Maples et al., 2013]). It focuses on efficient access to <code>.lanc</code> data and is not intended to replace the full functionality of Admix-kit.</p>","path":["lanctools"],"tags":[]},{"location":"#features","level":2,"title":"Features","text":"<ul> <li>Efficient random access to phased local ancestry data</li> <li>Local ancestry-masked genotype queries</li> <li>Conversion from FLARE and RFMix output to <code>.lanc</code> format</li> <li>Python API and command-line interface (CLI)</li> </ul>","path":["lanctools"],"tags":[]},{"location":"#installation","level":2,"title":"Installation","text":"<pre><code>pip install lanctools\n</code></pre>","path":["lanctools"],"tags":[]},{"location":"#quickstart","level":2,"title":"Quickstart","text":"","path":["lanctools"],"tags":[]},{"location":"#querying-local-ancestry-data","level":3,"title":"Querying Local Ancestry Data","text":"<pre><code>import numpy as np\nfrom lanctools import LancData\n\nld = LancData(\n    plink_prefix=\"chr1\",\n    lanc_file=\"chr1.lanc\",\n    ancestries=[\"YRI\", \"CEU\"]\n)\n\nidx_var = np.arange(100, dtype=np.uint32)\n\n# Get phased local ancestry: shape (N, 100, 2)\nlanc = ld.get_lanc(idx_var)\n\n# Get phased genotypes: shape (N, 100, 2)\ngeno = ld.get_geno(idx_var)\n\n# Get ancestry-masked genotypes: shape (N, 100, len(ancestries))\nlanc_geno = ld.get_lanc_geno(idx_var)\n</code></pre>","path":["lanctools"],"tags":[]},{"location":"#converting-flare-or-rfmix-files-to-lanc","level":3,"title":"Converting FLARE or RFMix Files to .lanc","text":"<pre><code>from lanctools import convert_to_lanc\n\nconvert_to_lanc(\n    file=\"chr1.anc.vcf.gz\",\n    file_fmt=\"FLARE\",\n    plink_prefix=\"chr1\",\n    output=\"chr1.lanc\"\n)\n</code></pre>","path":["lanctools"],"tags":[]},{"location":"#command-line-interface","level":3,"title":"Command-Line Interface","text":"<pre><code>lanctools convert-flare --file chr1.anc.vcf.gz --plink_prefix chr1 --output chr1.lanc\n</code></pre>","path":["lanctools"],"tags":[]},{"location":"#references","level":2,"title":"References","text":"<ul> <li>Hou, K. et al. Admix-kit: an integrated toolkit and pipeline for genetic analyses of admixed populations. Bioinformatics 40, btae148 (2024). paper software</li> <li>Browning, S. R., Waples, R. K. &amp; Browning, B. L. Fast, accurate local ancestry inference with FLARE. Am J Hum Genet 110, 326–335 (2023). paper software</li> <li>Maples, B. K., Gravel, S., Kenny, E. E. &amp; Bustamante, C. D. RFMix: A Discriminative Modeling Approach for Rapid and Robust Local-Ancestry Inference. Am J Hum Genet 93, 278–288 (2013). paper software</li> </ul>","path":["lanctools"],"tags":[]},{"location":"package/","level":1,"title":"Documentation for lanctools Python API","text":"","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#classes","level":2,"title":"Classes","text":"","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lancdata","level":3,"title":"LancData","text":"","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData","level":5,"title":"<code>lanctools.core.LancData</code>","text":"<p>The genotype and local ancestry data for a single chromosome/dataset.</p> <p>Attributes:     pgen (PgenReader): A pgenlib PgenReader object.     pvar: A pgenlib PVarReader object.     lanc: A FlatLanc object with local ancestry data.     ancestries: An ordered list of ancestry names. The integer codes in         the .lanc file and <code>self.lanc</code> correspond to indices in this list (e.g.         0 -&gt; ancestries[0]).     plink_prefix: The prefix for the corresponding plink2 fileset.</p> Source code in <code>src/lanctools/core.py</code> <pre><code>class LancData:\n    \"\"\"The genotype and local ancestry data for a single chromosome/dataset.\n\n    Attributes:\n        pgen (PgenReader): A pgenlib PgenReader object.\n        pvar: A pgenlib PVarReader object.\n        lanc: A FlatLanc object with local ancestry data.\n        ancestries: An ordered list of ancestry names. The integer codes in\n            the .lanc file and `self.lanc` correspond to indices in this list (e.g.\n            0 -&gt; ancestries[0]).\n        plink_prefix: The prefix for the corresponding plink2 fileset.\n    \"\"\"\n\n    def __init__(\n        self,\n        plink_prefix: str,\n        lanc_file: str,\n        ancestries: Optional[list[str]] = None,\n    ):\n        \"\"\"Constructs a LancData from plink2 files.\n\n        Args:\n            plink_prefix: A string with the prefix for a plink2 fileset.\n            lanc_file: A string with the path to a .lanc file.\n            ancestries: An optional list of ordered ancestry names corresponding to the .lanc file.\n        \"\"\"\n        pgen = PgenReader(bytes(plink_prefix + \".pgen\", \"utf8\"))\n        pvar = PvarReader(bytes(plink_prefix + \".pvar\", \"utf8\"))\n        lanc = _read_lanc(lanc_file)\n\n        if ancestries is None:\n            all_values = np.concatenate([lanc.left_haps, lanc.right_haps])\n            ancestries = [str(i) for i in np.unique(all_values)]\n\n        self.pgen = pgen\n        self.pvar = pvar\n        self.lanc = lanc\n        self.ancestries = ancestries\n        self.plink_prefix = plink_prefix\n\n    def get_info(self, indices: NDArray[np.uint32]) -&gt; DataFrame:\n        \"\"\"Query info for a set of variants.\n\n        Args:\n            indices: Array of variant indices in pvar order (0-based), shape\n                ``(V,)``, dtype ``int32``.\n\n        Returns:\n            A pandas ``DataFrame`` with one row per variant and the following columns:\n            - ``chrom`` (str): Chromosome name\n            - ``pos`` (uint32): 1-based genomic position\n            - ``ref`` (str): Reference allele\n            - ``alt`` (str): Alternate allele\n            - ``rsid`` (str): Variant identifier\n        \"\"\"\n\n        return _get_info(self.pvar, indices)\n\n    def get_lanc(self, indices: NDArray[np.unsignedinteger]) -&gt; NDArray[np.uint8]:\n        \"\"\"Query phased local ancestry.\n\n        :param indices: Array of variant indices in pvar order (0-based), shape\n            ``(V,)``, dtype ``int32``.\n        :type indices: numpy.ndarray\n        :return: Local ancestries, shape ``(N, V, 2)``, dtype ``uint8``\n        :rtype: numpy.ndarray\n        \"\"\"\n\n        left, right = _get_lanc(\n            self.lanc.left_haps,\n            self.lanc.right_haps,\n            self.lanc.breakpoints,\n            self.lanc.offsets,\n            indices,\n        )\n        return np.stack((left, right), axis=-1)\n\n    def get_lanc_dosage(self, indices: NDArray[np.uint32]) -&gt; NDArray[np.uint8]:\n        \"\"\"Query local ancestry dosage.\n\n        :param indices: Array of variant indices in pvar order (0-based), shape\n            ``(V,)``, dtype ``int32``.\n        :type indices: numpy.ndarray\n        :return: Local ancestry dosages, shape ``(N, V, len(self.ancestries))``, dtype ``uint8``.\n        :rtype: numpy.ndarray\n        \"\"\"\n\n        lanc = np.asarray(self.get_lanc(indices), dtype=np.uint8)\n        ancestries = np.arange(len(self.ancestries), dtype=np.uint8)\n        left_haps_mask = (lanc[:, :, 0:1] == ancestries[None, None, :]).astype(np.int32)\n        right_haps_mask = (lanc[:, :, 1:2] == ancestries[None, None, :]).astype(\n            np.int32\n        )\n        return left_haps_mask + right_haps_mask\n\n    def get_geno(self, indices: NDArray[np.uint32]) -&gt; NDArray[np.int32]:\n        \"\"\"Query phased genotypes.\n\n        :param indices: Array of variant indices in pvar order (0-based), shape\n            ``(V,)``, dtype ``int32``.\n        :type indices: numpy.ndarray\n        :return: Phased genotypes, shape ``(N, V, 2)``, dtype ``int32``.\n        :rtype: numpy.ndarray\n        \"\"\"\n\n        return _get_geno(self.pgen, indices)\n\n    def get_lanc_geno(self, indices: NDArray[np.unsignedinteger]) -&gt; NDArray[np.int32]:\n        \"\"\"Query genotypes deconvoluted/masked by ancestry.\n\n        :param indices: Array of variant indices in pvar order (0-based), shape\n            ``(V,)``, dtype ``int32``.\n        :type indices: numpy.ndarray\n        :return: Genotypes masked by ancestry, shape ``(N, V, len(self.ancestries))``, dtype ``int32``.\n        :rtype: numpy.ndarray\n        \"\"\"\n        geno = np.asarray(self.get_geno(indices), dtype=np.int32)\n        lanc = np.asarray(self.get_lanc(indices), dtype=np.uint8)\n        ancestries = np.arange(len(self.ancestries), dtype=np.uint8)\n        left_haps_mask = (lanc[:, :, 0:1] == ancestries[None, None, :]).astype(np.int32)\n        right_haps_mask = (lanc[:, :, 1:2] == ancestries[None, None, :]).astype(\n            np.int32\n        )\n        geno_masked = (\n            left_haps_mask * geno[:, :, 0:1] + right_haps_mask * geno[:, :, 1:2]\n        )\n        return geno_masked\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.__init__","level":6,"title":"<code>__init__(plink_prefix, lanc_file, ancestries=None)</code>","text":"<p>Constructs a LancData from plink2 files.</p> <p>Args:     plink_prefix: A string with the prefix for a plink2 fileset.     lanc_file: A string with the path to a .lanc file.     ancestries: An optional list of ordered ancestry names corresponding to the .lanc file.</p> Source code in <code>src/lanctools/core.py</code> <pre><code>def __init__(\n    self,\n    plink_prefix: str,\n    lanc_file: str,\n    ancestries: Optional[list[str]] = None,\n):\n    \"\"\"Constructs a LancData from plink2 files.\n\n    Args:\n        plink_prefix: A string with the prefix for a plink2 fileset.\n        lanc_file: A string with the path to a .lanc file.\n        ancestries: An optional list of ordered ancestry names corresponding to the .lanc file.\n    \"\"\"\n    pgen = PgenReader(bytes(plink_prefix + \".pgen\", \"utf8\"))\n    pvar = PvarReader(bytes(plink_prefix + \".pvar\", \"utf8\"))\n    lanc = _read_lanc(lanc_file)\n\n    if ancestries is None:\n        all_values = np.concatenate([lanc.left_haps, lanc.right_haps])\n        ancestries = [str(i) for i in np.unique(all_values)]\n\n    self.pgen = pgen\n    self.pvar = pvar\n    self.lanc = lanc\n    self.ancestries = ancestries\n    self.plink_prefix = plink_prefix\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.get_geno","level":6,"title":"<code>get_geno(indices)</code>","text":"<p>Query phased genotypes.</p> <p>Parameters:</p> Name Type Description Default <code>indices</code> <code>NDArray[uint32]</code> <p>Array of variant indices in pvar order (0-based), shape <code>(V,)</code>, dtype <code>int32</code>.</p> required <p>Returns:</p> Type Description <code>numpy.ndarray</code> <p>Phased genotypes, shape <code>(N, V, 2)</code>, dtype <code>int32</code>.</p> Source code in <code>src/lanctools/core.py</code> <pre><code>def get_geno(self, indices: NDArray[np.uint32]) -&gt; NDArray[np.int32]:\n    \"\"\"Query phased genotypes.\n\n    :param indices: Array of variant indices in pvar order (0-based), shape\n        ``(V,)``, dtype ``int32``.\n    :type indices: numpy.ndarray\n    :return: Phased genotypes, shape ``(N, V, 2)``, dtype ``int32``.\n    :rtype: numpy.ndarray\n    \"\"\"\n\n    return _get_geno(self.pgen, indices)\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.get_info","level":6,"title":"<code>get_info(indices)</code>","text":"<p>Query info for a set of variants.</p> <p>Args:     indices: Array of variant indices in pvar order (0-based), shape         <code>(V,)</code>, dtype <code>int32</code>.</p> <p>Returns:     A pandas <code>DataFrame</code> with one row per variant and the following columns:     - <code>chrom</code> (str): Chromosome name     - <code>pos</code> (uint32): 1-based genomic position     - <code>ref</code> (str): Reference allele     - <code>alt</code> (str): Alternate allele     - <code>rsid</code> (str): Variant identifier</p> Source code in <code>src/lanctools/core.py</code> <pre><code>def get_info(self, indices: NDArray[np.uint32]) -&gt; DataFrame:\n    \"\"\"Query info for a set of variants.\n\n    Args:\n        indices: Array of variant indices in pvar order (0-based), shape\n            ``(V,)``, dtype ``int32``.\n\n    Returns:\n        A pandas ``DataFrame`` with one row per variant and the following columns:\n        - ``chrom`` (str): Chromosome name\n        - ``pos`` (uint32): 1-based genomic position\n        - ``ref`` (str): Reference allele\n        - ``alt`` (str): Alternate allele\n        - ``rsid`` (str): Variant identifier\n    \"\"\"\n\n    return _get_info(self.pvar, indices)\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.get_lanc","level":6,"title":"<code>get_lanc(indices)</code>","text":"<p>Query phased local ancestry.</p> <p>Parameters:</p> Name Type Description Default <code>indices</code> <code>NDArray[unsignedinteger]</code> <p>Array of variant indices in pvar order (0-based), shape <code>(V,)</code>, dtype <code>int32</code>.</p> required <p>Returns:</p> Type Description <code>numpy.ndarray</code> <p>Local ancestries, shape <code>(N, V, 2)</code>, dtype <code>uint8</code></p> Source code in <code>src/lanctools/core.py</code> <pre><code>def get_lanc(self, indices: NDArray[np.unsignedinteger]) -&gt; NDArray[np.uint8]:\n    \"\"\"Query phased local ancestry.\n\n    :param indices: Array of variant indices in pvar order (0-based), shape\n        ``(V,)``, dtype ``int32``.\n    :type indices: numpy.ndarray\n    :return: Local ancestries, shape ``(N, V, 2)``, dtype ``uint8``\n    :rtype: numpy.ndarray\n    \"\"\"\n\n    left, right = _get_lanc(\n        self.lanc.left_haps,\n        self.lanc.right_haps,\n        self.lanc.breakpoints,\n        self.lanc.offsets,\n        indices,\n    )\n    return np.stack((left, right), axis=-1)\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.get_lanc_dosage","level":6,"title":"<code>get_lanc_dosage(indices)</code>","text":"<p>Query local ancestry dosage.</p> <p>Parameters:</p> Name Type Description Default <code>indices</code> <code>NDArray[uint32]</code> <p>Array of variant indices in pvar order (0-based), shape <code>(V,)</code>, dtype <code>int32</code>.</p> required <p>Returns:</p> Type Description <code>numpy.ndarray</code> <p>Local ancestry dosages, shape <code>(N, V, len(self.ancestries))</code>, dtype <code>uint8</code>.</p> Source code in <code>src/lanctools/core.py</code> <pre><code>def get_lanc_dosage(self, indices: NDArray[np.uint32]) -&gt; NDArray[np.uint8]:\n    \"\"\"Query local ancestry dosage.\n\n    :param indices: Array of variant indices in pvar order (0-based), shape\n        ``(V,)``, dtype ``int32``.\n    :type indices: numpy.ndarray\n    :return: Local ancestry dosages, shape ``(N, V, len(self.ancestries))``, dtype ``uint8``.\n    :rtype: numpy.ndarray\n    \"\"\"\n\n    lanc = np.asarray(self.get_lanc(indices), dtype=np.uint8)\n    ancestries = np.arange(len(self.ancestries), dtype=np.uint8)\n    left_haps_mask = (lanc[:, :, 0:1] == ancestries[None, None, :]).astype(np.int32)\n    right_haps_mask = (lanc[:, :, 1:2] == ancestries[None, None, :]).astype(\n        np.int32\n    )\n    return left_haps_mask + right_haps_mask\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]},{"location":"package/#lanctools.core.LancData.get_lanc_geno","level":6,"title":"<code>get_lanc_geno(indices)</code>","text":"<p>Query genotypes deconvoluted/masked by ancestry.</p> <p>Parameters:</p> Name Type Description Default <code>indices</code> <code>NDArray[unsignedinteger]</code> <p>Array of variant indices in pvar order (0-based), shape <code>(V,)</code>, dtype <code>int32</code>.</p> required <p>Returns:</p> Type Description <code>numpy.ndarray</code> <p>Genotypes masked by ancestry, shape <code>(N, V, len(self.ancestries))</code>, dtype <code>int32</code>.</p> Source code in <code>src/lanctools/core.py</code> <pre><code>def get_lanc_geno(self, indices: NDArray[np.unsignedinteger]) -&gt; NDArray[np.int32]:\n    \"\"\"Query genotypes deconvoluted/masked by ancestry.\n\n    :param indices: Array of variant indices in pvar order (0-based), shape\n        ``(V,)``, dtype ``int32``.\n    :type indices: numpy.ndarray\n    :return: Genotypes masked by ancestry, shape ``(N, V, len(self.ancestries))``, dtype ``int32``.\n    :rtype: numpy.ndarray\n    \"\"\"\n    geno = np.asarray(self.get_geno(indices), dtype=np.int32)\n    lanc = np.asarray(self.get_lanc(indices), dtype=np.uint8)\n    ancestries = np.arange(len(self.ancestries), dtype=np.uint8)\n    left_haps_mask = (lanc[:, :, 0:1] == ancestries[None, None, :]).astype(np.int32)\n    right_haps_mask = (lanc[:, :, 1:2] == ancestries[None, None, :]).astype(\n        np.int32\n    )\n    geno_masked = (\n        left_haps_mask * geno[:, :, 0:1] + right_haps_mask * geno[:, :, 1:2]\n    )\n    return geno_masked\n</code></pre>","path":["Documentation for lanctools Python API"],"tags":[]}]}